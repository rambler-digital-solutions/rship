'use strict';

// ======================
// Depends
// ======================
const { expect } = require('chai');
const { describe } = require('mocha');

// ======================
// Screens
// ======================
const dev = require('../screens/dev');

describe('dev screen', () => {
  it('dev should return blocks', () => {
    // console.log(dev({}, false));
  })

});
